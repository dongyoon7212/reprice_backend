<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.reprice_backend.mapper.ChatReadStatusMapper">

    <insert id="upsertChatReadStatus">
        insert into
            chat_read_status_tb
        values (
            0,
            #{chatRoomId},
            #{userId},
            #{lastReadMessageId},
            #{updateDt}
        )
        on duplicate key update
            chat_room_id = values(chat_room_id),
            last_read_message_id = values(last_read_message_id),
            update_dt = values(update_dt);
    </insert>
</mapper>